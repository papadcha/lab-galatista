-- ============================================================
--  MIGRATION 016 — Soft-delete + edit propagation στη Βιβλιοθήκη Εγγράφων
--  Ημερομηνία: 2026-07-05
--  Αναιρέσιμη με: δεν υπάρχει αυτόματο rollback (η στήλη μένει αν
--  χρειαστεί να αναιρεθεί χειροκίνητα με DROP COLUMN — SQLite 3.35+)
-- ============================================================
--
--  Πρόβλημα: το additions-only sync της βιβλιοθήκης εγγράφων (v1.1.11)
--  δεν διαδίδει edits/deletes μεταξύ εγκαταστάσεων — το
--  import_document_library() αγνοεί οποιοδήποτε cloud_path υπάρχει
--  ήδη τοπικά, ακόμα κι αν έχει ενημερωθεί αλλού. Και το
--  delete_document() κάνει hard DELETE, οπότε δεν υπάρχει τίποτα να
--  ανταλλαγεί ώστε η άλλη εγκατάσταση να μάθει για τη διαγραφή.
--
--  Fix: προσθήκη deleted_at (soft-delete tombstone). Το updated_at
--  υπάρχει ήδη στο schema από την αρχή. Το import_document_library()
--  αλλάζει σε πραγματικό upsert βάσει updated_at (last-write-wins).
-- ============================================================

ALTER TABLE tbl_documents ADD COLUMN deleted_at TEXT;
