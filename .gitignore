# ---- Node / Electron ----
node_modules/
npm-debug.log*
yarn-error.log*

# ---- Electron build output ----
dist/
dist-installer/
build/
out/
*.AppImage
*.dmg
*.deb
*.snap

# ---- Bundled rclone (binary — δεν ανήκει στο repo) ----
rclone/

# ---- Python ----
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
venv/
.venv/
env/

# ---- PyInstaller (Windows build) ----
*.spec.bak

# ---- Βάση δεδομένων (πραγματικά δεδομένα — backup μέσω rclone, rebuild αυτόματο από db_manager.py + seed_data.py) ----
*.db
*.db-journal
*.db-wal
*.db-shm
*.backup_before_*
database/laboratory.db.backup*

# ---- Runtime config (μηχανο-ειδικά paths + cloud remote) ----
lab-config.json

# ---- GitHub token για version-issue reporting (μπαίνει στο bundle κατά
#      το build, ΠΟΤΕ στο git — issues:write only, βλ. TODOLIST.md) ----
github-token.json

# ---- Bundled binaries ανά πλατφόρμα (κατεβαίνουν ξεχωριστά) ----
resources/python/
resources/rclone/

# ---- OS junk ----
.DS_Store
Thumbs.db
*~
*.swp
